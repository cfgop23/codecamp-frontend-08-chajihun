import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 50px;
  background-color: #f0f0f0;
`;

export default function LayoutNavigation() {
  return <Wrapper>내비게이션영역</Wrapper>;
}
